services:
  - type: web
    name: mineral-forecasting
    env: python
    plan: free   # or starter if you upgrade
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
